<%- include('partials/header', { title: 'Funcionários', paginaAtiva: 'funcionarios' }) %>

<header class="header">
    <h1 class="header-title">Gerenciamento de Funcionários</h1>
    <div class="header-right">
        <div class="notification-icon">
            <i class="bi bi-bell-fill"></i>
        </div>
        <div class="user-profile">
            <span>Bem-vindo, <%= currentUser.nome %></span>
            <img src="https://i.pravatar.cc/40?u=<%= currentUser.email %>" alt="Avatar">
        </div>
    </div>
</header>

<main class="main-content">

    <section class="inventory-table" style="margin-bottom: 30px;">
        <div class="main-header" style="margin-bottom: 15px;">
            <h2>Meu Perfil</h2>
        </div>
        <p><strong>Nome:</strong> <%= currentUser.nome %></p>
        <p><strong>Email:</strong> <%= currentUser.email %></p>
        <div style="margin-top: 20px;">
            <% if (currentUser.two_factor_enabled) { %>
                <p style="color: var(--success); font-weight: bold;">
                    <i class="bi bi-shield-check-fill"></i> Verificação em duas etapas está ATIVA.
                </p>
            <% } else { %>
                <p style="margin-bottom: 10px;">A verificação em duas etapas está desativada. Ative para maior segurança.</p>
                <a href="/ativacao/ativar-2fa" class="btn-primary" style="margin-top: 10px; text-decoration: none;">
                    <i class="bi bi-shield-lock-fill"></i> Ativar Verificação em Duas Etapas
                </a>
            <% } %>
        </div>
    </section>

    <div class="main-header">
        <h2>Todos os Funcionários Cadastrados</h2>
        <div class="action-buttons">
            <button class="btn-entrada"><i class="bi bi-plus-circle-fill"></i> Adicionar Novo Funcionário</button>
        </div>
    </div>
    <section class="inventory-table">
        <table>
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Cargo</th>
                    <th>Email</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody id="funcionarios-table-body">
                <% if (typeof funcionarios !== 'undefined' && funcionarios.length > 0) { %>
                    <% funcionarios.forEach(f => { %>
                        <tr>
                            <td><strong><%= f.nome %></strong></td>
                            <td><%= f.cargo %></td>
                            <td><%= f.email %></td>
                            <td>
                                <button title="Editar" class="action-btn"><i class="bi bi-pencil-square"></i></button>
                                <button title="Excluir" class="action-btn danger"><i class="bi bi-trash-fill"></i></button>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr><td colspan="4" style="text-align: center;">Nenhum funcionário encontrado.</td></tr>
                <% } %>
            </tbody>
        </table>
    </section>
</main>

<%- include('partials/footer') %>